# Codex Build (Homepage)

This folder contains the Codex homepage implementation for BestFoodWhere.sg.

## Dev

- `npm install`
- `npm run dev` (runs on `http://localhost:4007`)

## Checks

- `npm run lint`
- `npm run type-check`
- `npm run build`
